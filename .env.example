# 复制为 .env 后按需修改
APP_PORT=8080
GIN_MODE=debug

# 登录 cookie 签名密钥（HMAC-SHA256），务必改成随机长字符串
SESSION_SECRET=change-me-to-a-random-string

# 首次启动若数据库中无管理员，则按以下账密创建（之后修改密码需在数据库中改）
ADMIN_USER=admin
ADMIN_PASS=admin123

# SQLite 单文件路径
DB_PATH=./data/certlive.db